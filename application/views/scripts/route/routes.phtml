<?php
Zend_Debug::dump( $this->layout() );
exit;
$this->layout()->j->addBasicPage('', 'This is the route', 'It\'s works!');
$this->headTitle( "Routes" );
$this->addHelperPath(APPLICATION_PATH . '/views/helpers', 'Application_View_Helper_');
$routes = $this->routes;


foreach( $routes as $route ) {
    echo $this->routeHtmlHelper($route);
}